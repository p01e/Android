<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jayhind.lecture_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".toolbar.toolbarActivity"
            android:label="@string/title_activity_toolbar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Dialogs.ToastActivity" />
        <activity android:name=".Dialogs.AlertDialogActivity" />
        <activity android:name=".Dialogs.DatePicker_and_Time_picker_DialogActivity" />
        <activity android:name=".Adapter.ArrayAdapaterActivity" />
        <activity android:name=".Pass_data_fregments.passDataActivity" />
        <activity android:name=".Pass_data_fregments.fragToActiActivity" />
        <activity android:name=".database.sqlite.Fragements.databaseActivity" />
        <activity android:name=".Internal_External_storage.Internal.InternalFileStorageActivity" />
        <activity android:name=".Internal_External_storage.External.ExternalDatabaseActivity" />
        <activity android:name=".webviewdemo.WebViewActivity" />
        <activity android:name=".apidemo.ApiDemoActivity" />
        <activity android:name=".OnlineStorage.UserDatabaseActivity" />
        <activity android:name=".Retrofit.ClientActivity" />
        <activity android:name=".imageDemo.imageLoadingActivity" />
        <activity android:name=".imageDemo.imageDownloadingActivity"></activity>
        <activity android:name=".readyMadeSqliteDatabase.databaseActivity" />
        <activity android:name=".image_uploading.ulpoadingActivity">

        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".mapdemo.MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>